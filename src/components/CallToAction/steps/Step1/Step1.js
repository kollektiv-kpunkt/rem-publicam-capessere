import React from "react";
import PropTypes from "prop-types";
//import { Test } from './Step1.styles';

const Step1 = (props) => <div className="Step1Wrapper">Step1</div>;

Step1.propTypes = {
  // bla: PropTypes.string,
};

Step1.defaultProps = {
  // bla: 'test',
};

export default Step1;
