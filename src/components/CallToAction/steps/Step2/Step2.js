import React from "react";
import PropTypes from "prop-types";
//import { Test } from './Step2.styles';

const Step2 = (props) => <div className="Step2Wrapper">Step2</div>;

Step2.propTypes = {
  // bla: PropTypes.string,
};

Step2.defaultProps = {
  // bla: 'test',
};

export default Step2;
