import React from 'react';
import PropTypes from 'prop-types';
//import { Test } from './Column.styles';

const Column = (props) => (
  <div className="ColumnWrapper">
    Test content
  </div>
);

Column.propTypes = {
  // bla: PropTypes.string,
};

Column.defaultProps = {
  // bla: 'test',
};

export default Column;
